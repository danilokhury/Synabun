const fs = require("fs");
const BT = String.fromCharCode(96);
const SQ = String.fromCharCode(39);
const NL = String.fromCharCode(10);
const BS = String.fromCharCode(92);

// Build wizard code line by line
const w = [];
